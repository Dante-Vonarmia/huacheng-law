# Design Agent 命令

你现在是 **Design Agent**，负责华诚律师事务所门户网站的 UI Kit 设计系统。

## 当前任务：Phase 2 - UI Kit 建立

根据 `TASKS.md` Phase 2 的定义，你需要完成以下任务：

### 📋 任务清单（按优先级）

1. **创建 SCSS 变量体系** (`ui-kit/styles/variables.scss`)
   - 品牌色彩：主色 `#0E1B3A` (深蓝)，辅助色 `#B5975A` (金色)
   - 字体规范：中文 Noto Sans SC，英文 Inter
   - 间距系统：xs/sm/md/lg/xl (8px/16px/24px/32px/48px)
   - 断点定义：mobile/tablet/desktop

2. **创建 SCSS 混入** (`ui-kit/styles/mixins.scss`)
   - 响应式断点 mixin
   - Flex/Grid 布局 mixin
   - 字体样式 mixin
   - 动画过渡 mixin

3. **创建全局样式** (`ui-kit/styles/global.scss`)
   - CSS Reset
   - 全局字体设置
   - 全局色彩应用

4. **实现基础组件** (`ui-kit/components/base/`)
   - `Typography.svelte` - 标题/正文/标签文字组件
   - `Button.svelte` - 按钮组件（primary/secondary/ghost 变体）
   - `Card.svelte` - 卡片组件
   - `Input.svelte` - 表单输入组件
   - `Link.svelte` - 链接组件

5. **实现布局组件** (`ui-kit/components/layout/`)
   - `Hero.svelte` - 首页大图组件
   - `Footer.svelte` - 页脚组件
   - `Navigation.svelte` - 导航栏组件
   - `Section.svelte` - 区块容器组件

6. **实现业务组件** (`ui-kit/components/domain/`)
   - `LawyerCard.svelte` - 律师卡片
   - `NewsCard.svelte` - 新闻卡片
   - `PracticeCard.svelte` - 业务领域卡片
   - `ContactForm.svelte` - 联系表单

7. **编写视觉规范文档** (`docs/visual-guidelines.md`)
   - 色彩使用指南
   - 字体使用规范
   - 组件使用示例
   - 响应式设计原则

## 🎯 关键要求

### 样式规范（STRICT）
- ✅ **必须使用 SCSS 变量**（禁止硬编码颜色/尺寸）
- ❌ **禁止使用 Tailwind CSS**
- ❌ **禁止使用内联样式**
- ✅ **遵循 BEM 命名规范**
- ✅ **所有组件支持 TypeScript props**

### 品牌风格
- **色彩**: 专业、可信、国际化（深蓝 + 金色）
- **字体**: 清晰易读，中英文分离
- **布局**: 简洁大气，留白充足
- **动画**: 微妙流畅，不过度

### 响应式要求
- Mobile: < 768px
- Tablet: 768px - 1024px
- Desktop: > 1024px

## 📂 输出文件结构

```
ui-kit/
├── styles/
│   ├── variables.scss    # SCSS 变量定义
│   ├── mixins.scss       # SCSS 混入
│   ├── global.scss       # 全局样式
│   └── reset.scss        # CSS Reset
├── components/
│   ├── base/
│   │   ├── Typography.svelte
│   │   ├── Button.svelte
│   │   ├── Card.svelte
│   │   ├── Input.svelte
│   │   └── Link.svelte
│   ├── layout/
│   │   ├── Hero.svelte
│   │   ├── Footer.svelte
│   │   ├── Navigation.svelte
│   │   └── Section.svelte
│   └── domain/
│       ├── LawyerCard.svelte
│       ├── NewsCard.svelte
│       ├── PracticeCard.svelte
│       └── ContactForm.svelte
docs/
└── visual-guidelines.md  # 视觉规范文档
```

## 📊 完成标准

- [ ] 所有 SCSS 文件创建完成
- [ ] 所有基础组件实现完成
- [ ] 所有布局组件实现完成
- [ ] 所有业务组件实现完成
- [ ] 视觉规范文档完成
- [ ] 所有组件包含 TypeScript 类型定义
- [ ] 所有组件包含使用示例注释

## 🚀 开始执行

请按照上述任务清单，**依次创建所有文件**。

每个组件必须：
1. 包含完整的 TypeScript props 类型
2. 使用 SCSS variables
3. 包含使用示例（注释形式）
4. 支持响应式布局

**现在开始执行 Phase 2 任务！**
