# 华诚律师事务所门户 - 项目架构文档

**项目**: Watson & Band Law Firm Portal Revamp
**架构版本**: v1.0
**创建日期**: 2025-11-03

---

## 1. 架构概览

### 1.1 系统定位
华诚律师事务所门户网站是一个**内容展示型门户系统**，主要功能为：
- 展示律所信息（业务领域、律师团队、新闻动态）
- 提供多语言支持（中文 / English）
- 接收用户咨询（联系表单、活动报名）
- 提供全站搜索功能

### 1.2 技术架构
```
┌─────────────────────────────────────────────────────────┐
│                     用户浏览器                            │
│              (Desktop / Tablet / Mobile)                │
└───────────────────────┬─────────────────────────────────┘
                        │
                        │ HTTPS
                        │
┌───────────────────────▼─────────────────────────────────┐
│                  SvelteKit Frontend                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  SSG Pages   │  │  SSR Pages   │  │  CSR Parts   │  │
│  │  (Static)    │  │  (Dynamic)   │  │  (Interact)  │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │              UI Kit Components                    │  │
│  │     (Hero / Card / Footer / Navigation)          │  │
│  └──────────────────────────────────────────────────┘  │
└───────────────────────┬─────────────────────────────────┘
                        │
                        │ REST API
                        │
┌───────────────────────▼─────────────────────────────────┐
│                  Adapter Layer                           │
│  ┌──────────────────────────────────────────────────┐  │
│  │           schema-map.json (字段映射)              │  │
│  └──────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────┐  │
│  │           API Client (接口封装)                   │  │
│  └──────────────────────────────────────────────────┘  │
└───────────────────────┬─────────────────────────────────┘
                        │
                        │ HTTP/HTTPS
                        │
┌───────────────────────▼─────────────────────────────────┐
│                  Backend API (旧系统)                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │   Lawyers    │  │     News     │  │   Practices  │  │
│  │     API      │  │     API      │  │     API      │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │    Events    │  │   Contact    │  │    Search    │  │
│  │     API      │  │     API      │  │     API      │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└──────────────────────────────────────────────────────────┘
```

---

## 2. 目录结构规范

```
huacheng-law/
│
├── CLAUDE.md                     # 项目定义文件
├── AGENTS.md                     # Agent 配置文件
├── TASKS.md                      # 任务注册表
├── package.json                  # 依赖管理
├── tsconfig.json                 # TypeScript 配置
├── svelte.config.js              # SvelteKit 配置
├── vite.config.ts                # Vite 配置
├── .eslintrc.js                  # ESLint 配置
├── .prettierrc                   # Prettier 配置
│
├── docs/                         # 📚 项目文档
│   ├── architecture.md           # 架构文档 (本文件)
│   ├── project-progress.md       # 项目进度表
│   ├── visual-guidelines.md      # 视觉规范文档
│   ├── page-map.json             # 页面结构映射
│   └── entity-relations.json     # 实体关系映射
│
├── src/                          # 🎨 SvelteKit 源码
│   ├── routes/                   # 页面路由
│   │   ├── +page.svelte          # 首页
│   │   ├── +layout.svelte        # 全局布局
│   │   ├── about/
│   │   │   └── +page.svelte
│   │   ├── practices/
│   │   │   ├── +page.svelte      # 业务领域列表
│   │   │   └── [slug]/
│   │   │       └── +page.svelte  # 业务领域详情
│   │   ├── team/
│   │   │   ├── +page.svelte      # 律师列表
│   │   │   └── [id]/
│   │   │       └── +page.svelte  # 律师详情
│   │   ├── news/
│   │   │   ├── +page.svelte      # 新闻列表
│   │   │   └── [id]/
│   │   │       └── +page.svelte  # 新闻详情
│   │   ├── events/
│   │   │   ├── +page.svelte      # 活动列表
│   │   │   └── [id]/
│   │   │       └── +page.svelte  # 活动详情
│   │   ├── careers/
│   │   │   └── +page.svelte      # 招聘页面
│   │   ├── contact/
│   │   │   └── +page.svelte      # 联系我们
│   │   └── search/
│   │       └── +page.svelte      # 搜索结果
│   │
│   ├── lib/
│   │   ├── components/           # 通用组件
│   │   │   ├── Seo.svelte        # SEO Meta 组件
│   │   │   ├── LanguageSwitcher.svelte
│   │   │   └── Breadcrumb.svelte
│   │   ├── utils/                # 工具函数
│   │   │   ├── i18n.ts           # 多语言工具
│   │   │   ├── format.ts         # 格式化工具
│   │   │   └── seo.ts            # SEO 工具
│   │   └── types/                # TypeScript 类型
│   │       ├── lawyer.ts
│   │       ├── news.ts
│   │       ├── practice.ts
│   │       └── event.ts
│   │
│   ├── app.html                  # HTML 模板
│   └── app.css                   # 全局样式入口
│
├── ui-kit/                       # 🧩 UI 组件库
│   ├── components/
│   │   ├── base/                 # 基础组件
│   │   │   ├── Typography.svelte
│   │   │   ├── Button.svelte
│   │   │   ├── Card.svelte
│   │   │   ├── Input.svelte
│   │   │   └── Link.svelte
│   │   ├── layout/               # 布局组件
│   │   │   ├── Hero.svelte
│   │   │   ├── Footer.svelte
│   │   │   ├── Navigation.svelte
│   │   │   └── Section.svelte
│   │   └── domain/               # 业务组件
│   │       ├── LawyerCard.svelte
│   │       ├── NewsCard.svelte
│   │       ├── PracticeCard.svelte
│   │       └── ContactForm.svelte
│   │
│   └── styles/                   # 样式体系
│       ├── variables.scss        # SCSS 变量
│       ├── mixins.scss           # SCSS 混入
│       ├── global.scss           # 全局样式
│       └── reset.scss            # CSS Reset
│
├── adapter/                      # 🔌 后台适配层
│   ├── schema-map.json           # 字段映射表
│   ├── api-client.ts             # API 客户端封装
│   ├── types.ts                  # 后台数据类型
│   └── mock-data/                # 模拟数据 (开发阶段)
│       ├── lawyers.json
│       ├── news.json
│       ├── practices.json
│       └── events.json
│
├── hooks/                        # 🔧 增值功能接口 (预留)
│   ├── mirror.ts                 # 国际镜像部署
│   ├── seo-report.ts             # SEO 报告生成
│   └── monitor.ts                # 性能监控
│
├── locales/                      # 🌍 多语言配置
│   ├── zh.json                   # 简体中文
│   └── en.json                   # English
│
├── qa/                           # ✅ 质量保障
│   ├── scripts/                  # 测试脚本
│   │   ├── lighthouse.js         # Lighthouse 测试
│   │   ├── a11y-check.js         # A11y 检测
│   │   └── seo-check.js          # SEO 验证
│   └── reports/                  # 测试报告输出
│       ├── lighthouse-summary.json
│       ├── a11y-report.json
│       ├── seo-report.json
│       └── final-qa-report.md
│
└── static/                       # 📦 静态资源
    ├── images/                   # 图片资源
    │   ├── logo.svg
    │   ├── hero-banner.jpg
    │   └── lawyers/              # 律师照片
    ├── fonts/                    # 字体文件
    └── favicon.ico
```

---

## 3. 模块职责

### 3.1 src/routes (页面路由)
**职责**: 定义所有页面模板与路由逻辑

**规范**:
- 使用 SvelteKit 文件路由规范
- 动态路由使用 `[param]` 命名
- 每个页面必须包含 SEO Meta 信息
- 数据加载使用 `+page.ts` 或 `+page.server.ts`

**示例**:
```svelte
<!-- src/routes/team/[id]/+page.svelte -->
<script lang="ts">
  import { LawyerCard } from '$ui-kit/components/domain';
  export let data;
</script>

<LawyerCard lawyer={data.lawyer} />
```

---

### 3.2 ui-kit (UI 组件库)
**职责**: 提供可复用的 UI 组件与样式体系

**规范**:
- 所有组件使用 **PascalCase** 命名
- 所有样式使用 **SCSS 变量体系**
- 组件必须包含 **props 类型定义**
- 禁止使用内联样式或 Tailwind

**示例**:
```svelte
<!-- ui-kit/components/base/Button.svelte -->
<script lang="ts">
  export let variant: 'primary' | 'secondary' = 'primary';
  export let size: 'sm' | 'md' | 'lg' = 'md';
</script>

<button class="btn btn--{variant} btn--{size}">
  <slot />
</button>

<style lang="scss">
@import '$ui-kit/styles/variables.scss';

.btn {
  padding: $spacing-sm $spacing-md;
  background: $color-primary;
  color: white;

  &--secondary {
    background: $color-secondary;
  }
}
</style>
```

---

### 3.3 adapter (后台适配层)
**职责**: 封装后台 API 调用，处理字段映射

**核心文件**:
- `schema-map.json`: 定义后台字段到前端字段的映射关系
- `api-client.ts`: 封装所有 API 请求
- `types.ts`: 定义后台数据类型

**示例**:
```typescript
// adapter/api-client.ts
import type { Lawyer, Practice } from './types';
import schemaMap from './schema-map.json';

export async function fetchLawyers(): Promise<Lawyer[]> {
  const response = await fetch('/api/lawyers');
  const rawData = await response.json();

  // 使用 schema-map 转换字段
  return rawData.map(item => transformFields(item, schemaMap.lawyer));
}
```

---

### 3.4 locales (多语言)
**职责**: 管理中英文语言包

**规范**:
- 使用 JSON 格式存储翻译
- 键名使用点分命名 (eg. `nav.home`)
- 中英文必须保持结构一致

**示例**:
```json
// locales/zh.json
{
  "nav": {
    "home": "首页",
    "about": "关于我们",
    "practices": "业务领域"
  }
}

// locales/en.json
{
  "nav": {
    "home": "Home",
    "about": "About Us",
    "practices": "Practice Areas"
  }
}
```

---

### 3.5 qa (质量保障)
**职责**: 执行自动化测试与生成报告

**测试类型**:
1. **Lighthouse**: 性能 / SEO / A11y / Best Practices
2. **A11y**: ARIA 标签 / 色彩对比度 / 键盘导航
3. **SEO**: Meta 标签 / Sitemap / Structured Data

**执行方式**:
```bash
npm run test:lighthouse  # 运行 Lighthouse 测试
npm run test:a11y        # 运行 A11y 检测
npm run test:seo         # 运行 SEO 验证
npm run test:all         # 运行所有测试
```

---

## 4. 数据流

### 4.1 静态数据流 (SSG)
```
后台 API → Mock Data → SvelteKit Build → Static HTML
```
**适用页面**: 关于我们、业务领域列表

---

### 4.2 动态数据流 (SSR)
```
用户请求 → SvelteKit Server → Adapter Layer → 后台 API → 返回 HTML
```
**适用页面**: 律师详情、新闻详情

---

### 4.3 客户端数据流 (CSR)
```
用户操作 → 前端 JS → Adapter Layer → 后台 API → 更新 DOM
```
**适用功能**: 搜索、表单提交

---

## 5. 渲染策略

| 页面类型 | 渲染方式 | 理由 |
|---------|---------|------|
| 首页 | SSG | 内容固定，SEO 友好 |
| 关于我们 | SSG | 内容固定，SEO 友好 |
| 业务领域列表 | SSG | 分类固定，SEO 友好 |
| 业务领域详情 | SSG | 数据稳定，SEO 友好 |
| 律师列表 | SSR | 需要筛选功能 |
| 律师详情 | SSR | 内容动态更新 |
| 新闻列表 | SSR | 内容频繁更新 |
| 新闻详情 | SSR | 内容动态更新 |
| 搜索结果 | CSR | 用户交互驱动 |
| 联系表单 | CSR | 表单提交 |

---

## 6. 性能优化策略

### 6.1 图片优化
- 使用 WebP 格式 (降级 JPG/PNG)
- 实现懒加载 (Intersection Observer)
- 使用响应式图片 (`srcset`)

### 6.2 代码分割
- 按路由自动分割 (SvelteKit 默认)
- 动态 import 大型组件
- 提取公共依赖 (Vite)

### 6.3 CSS 优化
- 使用 SCSS 模块化
- 移除未使用的样式 (PurgeCSS)
- 压缩 CSS (cssnano)

### 6.4 缓存策略
- 静态资源使用强缓存 (1 年)
- API 响应使用协商缓存 (ETag)
- Service Worker 离线缓存 (可选)

---

## 7. SEO 优化策略

### 7.1 Meta 标签
每个页面必须包含：
- `<title>` (唯一)
- `<meta name="description">`
- `<meta property="og:*">` (Open Graph)
- `<link rel="canonical">`

### 7.2 多语言 SEO
- 使用 `<link rel="alternate" hreflang="zh-CN">`
- URL 结构: `/zh/...` 与 `/en/...`
- 生成多语言 Sitemap

### 7.3 结构化数据
使用 JSON-LD 格式：
- `Organization` (律所信息)
- `Person` (律师信息)
- `Article` (新闻文章)

---

## 8. 安全规范

### 8.1 输入验证
- 所有表单输入必须前后端双重验证
- 防止 XSS 攻击 (Svelte 自动转义)
- 防止 CSRF 攻击 (SvelteKit CSRF Token)

### 8.2 数据传输
- 全站 HTTPS
- API 请求使用 CORS 白名单
- 敏感数据加密传输

---

## 9. 浏览器兼容性

### 9.1 目标浏览器
- Chrome (最新版本 + 1 个旧版本)
- Firefox (最新版本 + 1 个旧版本)
- Safari (最新版本 + 1 个旧版本)
- Edge (最新版本)
- 移动端: iOS Safari 14+, Chrome Android 100+

### 9.2 降级策略
- 使用 PostCSS 自动添加浏览器前缀
- 使用 `@vitejs/plugin-legacy` 支持旧浏览器
- 提供 JavaScript 不可用时的基础内容

---

## 10. 部署架构

### 10.1 推荐部署方式
```
用户 → CDN (静态资源) → Vercel/Netlify (SvelteKit SSR) → 后台 API
```

### 10.2 国际镜像部署 (增值服务)
```
国内用户 → 阿里云 CDN → 国内服务器
海外用户 → Cloudflare CDN → AWS/Vercel
```

---

## 11. 监控与维护

### 11.1 性能监控
- Lighthouse CI (定期自动测试)
- Real User Monitoring (真实用户数据)
- Error Tracking (Sentry)

### 11.2 日志管理
- 前端错误日志 (console.error → 服务端)
- API 请求日志
- 用户行为分析 (可选)

---

**最后更新**: 2025-11-03
**文档版本**: v1.0
